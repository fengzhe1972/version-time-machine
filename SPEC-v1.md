# 版本时光机 v1 · 产品规格书（Spec）

> 版本: v1 ｜ 日期: 2026-09-08 ｜ 作者: 学习者（D36 练习产出）
> 用途: 作为 D37~D39 三天开发（骨架 → 核心功能 → 打磨）的施工图纸
> 技术形态: 纯前端单页（HTML+CSS+JS），数据存浏览器 localStorage，无后端、无账号——与灵感站同技术栈

## 1. 目标

给"我"用的文案版本管理页（电脑浏览器）。写文案/让 AI 改文案之前，把当前版本存进来；改完再存一版；任何时候能找回旧版本。

- 核心痛点: 文案改来改去，旧版本找不回来
- 关键场景: 在 Codex 里让 AI 反复改一段文案（口播稿/脚本/说明），改坏或想换风格时，找回旧版
- v0 承诺: **可以找回旧的版本**（覆盖"存版本 → 翻历史 → 一键复制取回"这一件事）
- 数据哲学: v0 不做删除——误删 = 另一种"找不回来"

## 2. 概念与数据模型（施工核心）

一条"稿子" = 一个文件夹：里面有一个名字 + 一摞**不可修改**的版本卡片。
每次"存为新版本"只是往文件夹里放一张新卡片，**从不改动旧卡片**——这就是"找回旧版"的保证。

```js
稿子 = {
  id: 数字（自动递增，不重复）,
  名字: "数字人开场白 0908",        // 空则存成"未命名稿子"
  创建时间: "2026-09-08 10:00",
  版本们: [
    { 版号: 1, 内容: "...初稿全文...", 时间: "2026-09-08 10:00", 备注: "初稿" },
    { 版号: 2, 内容: "...口语版...",   时间: "2026-09-08 10:30", 备注: "口语化" }
  ]
}
```

所有稿子组成一个数组，整个数组作为**一个值**存在 localStorage 的一个 key 里（如 `draftBox`）。
读取 = JSON.parse 整个数组；保存 = 把整个数组 JSON.stringify 后写入。怎么存就怎么取。

## 3. 功能清单

- **F1 稿子列表（主页）**: 顶部「新建稿子」按钮；下方每条稿子显示 名字 + 最新一版的时间与摘要（前 20 字）+ 版本总数。载入时从 localStorage 读出数组渲染。
- **F2 新建稿子**: 填名字（可空）+ 粘贴第一版内容 + 可选备注 → 点「保存」→ 生成稿子与版本1 → 进入详情页。
- **F3 详情页·当前编辑区**: 上方大编辑框显示最新版内容，可继续修改；下方「存为新版本」按钮 + 备注输入框（可选填）。
- **F4 存为新版本**: 点击 → 编辑框当前内容 push 为下一版（自动记时间、备注可选）→ 提示「已存为第 N 版」→ **清空编辑框**（防止连点存出重复版）。
- **F5 版本历史**: 详情页下方按版号**倒序**列出全部版本卡片：版号 + 时间 + 备注（无则显示"无备注"）+ 内容前 40 字摘要。点击卡片展开全文 + 出现「复制此版本」按钮。
- **F6 复制**: 点「复制此版本」→ 该版本完整内容进入剪贴板 → 按钮短暂变为「已复制 ✓」。
- **F7 导出备份**: 列表页放「导出全部数据」→ 把整个数组 stringify 后下载为 JSON 文件（文件名如 `draftbox-2026-09-08.json`）。
- **F8 返回**: 详情页左上「← 返回列表」。

## 4. 边界与处理规则（如果…怎么办 —— 写给施工的 AI）

- 版本一旦存入，**不提供修改/删除入口**（时间胶囊）。
- v0 不做: 删除稿子/删除版本、分类分组、全文搜索、多设备同步、自动保存、账号登录。
- 编辑框为空 → 禁止「存为新版本」，提示"内容为空"。
- 内容与最新一版完全相同 → 禁止保存，提示"没有改动"（防连点出重复版）。
- 备注可选填；不填历史里显示"无备注"。
- 稿子名字为空 → 保存时自动填"未命名稿子"。
- 刷新/关闭页面 → localStorage 数据不丢；再次打开读同一个 key。
- 一条稿子也没有 → 列表页显示空状态提示（如"还没有稿子，点上面新建"）。
- localStorage 存满 / 浏览器禁用存储 → v0 不处理（超出范围）。
- 剪贴板不可用（老浏览器/非安全上下文）→ 提示复制失败，用户可手动全选复制兜底。

## 5. 验收清单（亲手可测: 动作 → 期望）

1. 新建稿子"数字人开场白"，贴入初稿 → 保存 → 进入详情页，显示版本 1。
2. 返回列表 → 列表出现该稿子（名字 + 版本1 摘要）。
3. 详情页改动内容、备注填"口语化" → 存为新版本 → 提示"已存为第 2 版"，编辑框清空。
4. 内容没改直接点「存为新版本」→ 被拦，提示无改动。
5. 版本历史点版本 1 → 展开 → 复制 → 粘贴出来是初稿原文。
6. 点「导出全部数据」→ 下载 JSON → 打开文件能看到两个版本的完整内容。
7. 刷新页面 → 稿子与版本全部还在。
8. 编辑框清空后点保存 → 被拦，提示内容为空。

---
*开发排期参考: D37 骨架（F1/F3 页面 + 数据模型读写）→ D38 核心（F2/F4/F5/F6）→ D39 打磨（F7/F8 + 空态/提示/样式）*
